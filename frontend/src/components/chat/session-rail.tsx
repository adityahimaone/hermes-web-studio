import { Archive, ArchiveX, ChevronLeft, Pencil, Pin, Search, Trash2 } from 'lucide-react'
import { useState } from 'react'
import { Button } from '../ui/button'
import { Input } from '../ui/input'
import { Dialog } from '../ui/dialog'
import { cn } from '../../lib/cn'
import { filterSessions, groupSessionsByDate, type SessionSummary } from '../../lib/chat-contract'

type Props = { sessions: SessionSummary[]; activeSessionId: string; onSelectSession: (id: string) => void; onRename: (id: string, title: string) => Promise<void>; onPin: (id: string, pinned: boolean) => void; onArchive: (id: string, archived: boolean) => void; onDelete: (id: string) => Promise<void>; loading: boolean; error?: string; onToggle: () => void }
export function SessionRail({ sessions, activeSessionId, onSelectSession, onRename, onPin, onArchive, onDelete, loading, error, onToggle }: Props) {
  const [query, setQuery] = useState(''); const [renameTarget, setRenameTarget] = useState<SessionSummary | null>(null); const [deleteTarget, setDeleteTarget] = useState<SessionSummary | null>(null); const [renameValue, setRenameValue] = useState('')
  const groups = groupSessionsByDate(filterSessions(sessions, query))
  return <><aside className="hidden w-[336px] shrink-0 flex-col border-r bg-card/25 p-3 lg:flex" aria-label="Recent sessions"><div className="flex items-center gap-2 border-b pb-3"><div className="min-w-0"><p className="text-[11px] font-semibold uppercase tracking-[0.16em] text-foreground">Chat</p><p className="mt-0.5 text-[11px] text-muted-foreground">Recent sessions</p></div><Button variant="ghost" size="icon" className="ml-auto size-8 shrink-0" onClick={onToggle} aria-label="Collapse session sidebar"><ChevronLeft size={16} /></Button></div><div className="relative mt-3"><Search size={14} className="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground" /><Input id="session-search" value={query} onChange={event => setQuery(event.target.value)} placeholder="Search sessions" className="h-9 bg-background/50 pl-9 text-xs" /></div><div className="thin-scrollbar mt-2 min-h-0 flex-1 space-y-1 overflow-y-auto">{loading && <p className="px-3 py-2 text-xs text-muted-foreground" role="status">Loading session history...</p>}{error && <p className="px-3 py-2 text-xs text-destructive" role="alert">{error}</p>}{!loading && !error && !sessions.length && <p className="px-3 py-2 text-xs text-muted-foreground">No saved sessions yet.</p>}{!loading && !error && groups.map(group => <div key={group.label} className="pt-2"><p className="px-3 pb-1 text-[10px] font-medium uppercase tracking-[0.14em] text-muted-foreground">{group.label}</p>{group.sessions.map(item => <div key={item.session_id} className={cn('group relative flex min-h-11 items-center rounded-lg px-2 py-1 hover:bg-accent/70', item.session_id === activeSessionId && 'bg-accent/60')}><Button type="button" variant="ghost" size="sm" onClick={() => onSelectSession(item.session_id)} aria-current={item.session_id === activeSessionId ? 'page' : undefined} className="h-auto min-w-0 flex-1 justify-start gap-2 rounded-md px-1 py-1 pr-32 text-left text-sm"><Archive size={14} className="shrink-0 text-muted-foreground" /><span className="min-w-0 flex-1"><span className="block truncate">{item.title || 'Untitled session'}</span>{(item.project || item.project_id || item.tags?.length) && <span className="block truncate text-[10px] text-muted-foreground">{[item.project || item.project_id, ...(item.tags || [])].filter(Boolean).join(' · ')}</span>}</span>{item.pinned && <Pin size={12} className="shrink-0 text-primary" />}</Button><div className="absolute right-1 top-1/2 flex -translate-y-1/2 rounded-md bg-card/95 opacity-0 shadow-sm transition-opacity group-hover:opacity-100 group-focus-within:opacity-100"><Button type="button" variant="ghost" size="icon" className="size-7" onClick={() => { setRenameTarget(item); setRenameValue(item.title || '') }} aria-label={`Rename ${item.title || 'session'}`}><Pencil size={12} /></Button><Button type="button" variant="ghost" size="icon" className="size-7" onClick={() => onPin(item.session_id, !item.pinned)} aria-label={item.pinned ? `Unpin ${item.title || 'session'}` : `Pin ${item.title || 'session'}`}><Pin size={12} /></Button><Button type="button" variant="ghost" size="icon" className="size-7" onClick={() => onArchive(item.session_id, !item.archived)} aria-label={item.archived ? `Unarchive ${item.title || 'session'}` : `Archive ${item.title || 'session'}`}>{item.archived ? <Archive size={12} /> : <ArchiveX size={12} />}</Button><Button type="button" variant="ghost" size="icon" className="size-7 hover:bg-destructive/20 hover:text-destructive" onClick={() => setDeleteTarget(item)} aria-label={`Delete ${item.title || 'session'}`}><Trash2 size={12} /></Button></div></div>)}</div>)}{!loading && !error && sessions.length > 0 && groups.length === 0 && <p className="px-3 py-2 text-xs text-muted-foreground">No matching sessions.</p>}</div></aside><Dialog open={Boolean(renameTarget)} title="Rename session" onClose={() => setRenameTarget(null)}><form className="grid gap-3" onSubmit={event => { event.preventDefault(); if (renameTarget && renameValue.trim()) { void onRename(renameTarget.session_id, renameValue.trim()); setRenameTarget(null) } }}><Input autoFocus value={renameValue} onChange={event => setRenameValue(event.target.value)} placeholder="Session name" aria-label="Session name" /><div className="flex justify-end gap-2"><Button type="button" variant="outline" onClick={() => setRenameTarget(null)}>Cancel</Button><Button type="submit" disabled={!renameValue.trim()}>Rename</Button></div></form></Dialog><Dialog open={Boolean(deleteTarget)} title="Delete session?" onClose={() => setDeleteTarget(null)}><p className="text-sm text-muted-foreground">This removes the saved session and its transcript.</p><div className="mt-4 flex justify-end gap-2"><Button type="button" variant="outline" onClick={() => setDeleteTarget(null)}>Cancel</Button><Button type="button" onClick={() => { if (deleteTarget) void onDelete(deleteTarget.session_id); setDeleteTarget(null) }}>Delete session</Button></div></Dialog></>
}

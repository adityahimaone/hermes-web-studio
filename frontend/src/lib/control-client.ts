export type ControlItem = { id: string; title: string; description?: string; status: string; due_at?: string; created_at: string; updated_at: string; metadata?: Record<string, string> }
export async function getControl(collection: string) { const response = await fetch(`/api/control/${collection}`); const data = await response.json(); if (!response.ok) throw new Error(data.message || 'Control center unavailable'); return Array.isArray(data.items) ? data.items as ControlItem[] : [] }
export async function createControl(collection: string, title: string) { const response = await fetch(`/api/control/${collection}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ title }) }); const data = await response.json(); if (!response.ok) throw new Error(data.message || 'Could not create item'); return data as ControlItem }
export async function updateControl(collection: string, id: string, status: string) { const response = await fetch(`/api/control/${collection}/${id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ status }) }); if (!response.ok) throw new Error('Could not update item') }
export async function deleteControl(collection: string, id: string) { const response = await fetch(`/api/control/${collection}/${id}`, { method: 'DELETE' }); if (!response.ok) throw new Error('Could not delete item') }
